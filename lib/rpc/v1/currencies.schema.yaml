title: Currency lookup
permissions: ["*"]

methods:
  get:
    title: Get currency by iso3, symbol or name
    params:
      - name: id
        required: true
        schema:
          type: string

  get-all:
    title: Get all currencies
