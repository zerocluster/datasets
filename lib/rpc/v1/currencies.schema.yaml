title: Currency lookup
permissions: ["*"]

methods:
  get:
    title: Get currency by ISO code, symbol or name
    params:
      - name: id
        description: Currency ISO alpha-3 code, symbol or name.
        required: true
        schema:
          type: string

  getAll:
    title: Get all currencies
