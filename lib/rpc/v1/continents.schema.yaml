title: Continent lookup
permissions: ["*"]

methods:
  get:
    title: Get continent by ISO2 or name
    params:
      - name: id
        description: Continent ISO alpha-2 code or name.
        required: true
        schema:
          type: string

  get-all:
    title: Get all continents
