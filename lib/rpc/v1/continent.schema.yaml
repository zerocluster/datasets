title: Continent lookup
permissions: ["*"]

methods:
  get:
    title: Get continent by iso2 or name
    params:
      - name: id
        required: true
        schema:
          type: string

  get-all:
    title: Get all continents
