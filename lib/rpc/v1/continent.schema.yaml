summary: Continent lookup
permissions: ["*"]

methods:
  get:
    summary: Get continent by iso2 or name
    params:
      - name: id
        required: true
        schema:
          type: string

  get-all:
    summary: Get all continents
