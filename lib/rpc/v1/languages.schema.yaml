title: Language lookup
permissions: ["*"]

methods:
  get:
    title: Get language by ISO code or name
    params:
      - name: id
        description: Language ISO alpha-2, ISO alpha-3 code or name.
        required: true
        schema:
          type: string

  getAll:
    title: Get all languages
