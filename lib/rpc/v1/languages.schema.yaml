title: Language lookup
permissions: ["*"]

methods:
  get:
    title: Get language by iso2, iso3 or name
    params:
      - name: id
        required: true
        schema:
          type: string

  get-all:
    title: Get all languages
