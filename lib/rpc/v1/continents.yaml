title: Continent lookup

methods:
  get:
    title: Get continent by ISO code or name
    params:
      - name: id
        description: Continent ISO alpha-2 code or name.
        required: true
        schema:
          type: string

  get-all:
    title: Get all continents
