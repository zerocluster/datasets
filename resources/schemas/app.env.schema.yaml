type: object
properties:
  env:
    type: object
    properties:
      APP_GOOGLE_API_KEY: { type: string }
      APP_MAXMIND_LICENSE_KEY: { type: string }
      APP_GITHUB_TOKEN: { type: string }
    required: [APP_GOOGLE_API_KEY]
required: [env]
additionalProperties: false
